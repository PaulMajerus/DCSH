% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dimLoad.R
\name{dimLoad}
\alias{dimLoad}
\title{Chargement des tables dimensionnelles standardisées depuis la base \code{dcshdb}}
\usage{
dimLoad()
}
\arguments{
\item{x}{Un paramètre non utilisé (prévu pour compatibilité ou extension future).}
}
\value{
Une liste nommée de \code{data.frame}, chaque élément correspondant à une table dimensionnelle importée, avec des noms nettoyés en camelCase via \code{janitor::make_clean_names}.
}
\description{
Cette fonction se connecte à la base de données \code{dcshdb} via ODBC, récupère les tables dont le nom commence par \code{dim_}, applique une table de conversion pour sélectionner et renommer certaines tables selon une taxonomie standard, puis les importe toutes sous forme de liste de data.frames nommés.
}
\details{
La fonction effectue les étapes suivantes :
\itemize{
\item Connexion à la base de données \code{dcshdb} via le DSN ODBC.
\item Liste toutes les tables disponibles.
\item Filtre celles dont le nom commence par \code{dim_}.
\item Applique une table de conversion pour mapper les noms internes vers une taxonomie ciblée (et filtre les noms à exclure).
\item Exécute un \verb{SELECT *} pour chaque table sélectionnée.
\item Retourne les résultats dans une liste nommée.
}
}
\examples{
\dontrun{
dims <- dimLoad()
names(dims)
head(dims$dimPrta)
}

}
